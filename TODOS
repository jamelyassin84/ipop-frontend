animations
no data available
responsive tables
form validation
editable forms

non super admin UI
approval approve or reject on bulk
delete approve or reject on bulk
chart approvals
update all approvals

route state ngrx
route active sidebar
paginations
logout route
change role
responsive table
editable charts
polish design white space margin padding and colors and allignments for user perspecitve

animations
remove all errors
Edit All (Optional)
kuhaon ang data.data
service worker (Optional)
test with real data fetch provincial data

ng build --aot --build-optimizer --common-chunk --extract-licenses --localize --named-chunks --optimization --preserve-symlinks   --subresource-integrity --vendor-chunk