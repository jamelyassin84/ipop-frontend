additional routes sa home para ka pili
back to top
non super admin UI
chart approvals

update all approvals
form validation
approval approve or reject on bulk
delete approve or reject on bulk
route state ngrx
route active sidebar
paginations
fix padding
polish design white space margin padding and colors and allignments for user perspecitve
logout route
odometer (optional)
change role
responsive table
editable charts

animations
remove all errors
Edit All (Optional)
kuhaon ang data.data
service worker (Optional)
test with real data fetch provincial data


ng build --aot --build-optimizer --common-chunk --delete-output-path --extract-licenses --localize --named-chunks --optimization --preserve-symlinks   --subresource-integrity --vendor-chunk